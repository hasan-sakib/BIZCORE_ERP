<?xml version="1.0" encoding="UTF-8"?>
<phpunit
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xsi:noNamespaceSchemaLocation="https://schema.phpunit.de/11.0/phpunit.xsd"

    bootstrap="vendor/autoload.php"

    cacheDirectory=".phpunit.cache"
    executionOrder="depends,defects"
    requireCoverageMetadata="false"
    beStrictAboutCoverageMetadata="false"

    colors="true"
    displayDetailsOnIncompleteTests="true"
    displayDetailsOnSkippedTests="true"
    displayDetailsOnTestsThatTriggerDeprecations="true"
    displayDetailsOnTestsThatTriggerErrors="true"
    displayDetailsOnTestsThatTriggerNotices="true"
    displayDetailsOnTestsThatTriggerWarnings="true"
    displayDetailsOnPhpunitDeprecations="true"

    failOnEmptyTestSuite="true"
    failOnIncomplete="false"
    failOnRisky="false"
    failOnSkipped="false"
    failOnWarning="false"

    stopOnFailure="false"
    stopOnError="false"

    testdox="false">

    <!-- ======================================================================
         Test Suites
    ====================================================================== -->
    <testsuites>
        <testsuite name="Unit">
            <directory>tests/Unit</directory>
        </testsuite>
        <testsuite name="Feature">
            <directory>tests/Feature</directory>
        </testsuite>
    </testsuites>

    <!-- ======================================================================
         Code Coverage
         Target: 80%+ line coverage on the app/ source tree.
         Run with: phpunit -coverage-html storage/coverage
    ====================================================================== -->
    <source>
        <include>
            <directory suffix=".php">app</directory>
        </include>
        <exclude>
            <!-- Exclude generated / bootstrapping files from coverage metrics -->
            <directory>app/Providers</directory>
            <directory>app/Facades</directory>
            <directory>app/Foundation</directory>
        </exclude>
    </source>

    <!-- ======================================================================
         Environment Variables
         These override any .env file during test runs.
    ====================================================================== -->
    <php>
        <!-- Application -->
        <env name="APP_NAME"     value="BizCore ERP Test"/>
        <env name="APP_ENV"      value="testing"/>
        <env name="APP_DEBUG"    value="true"/>
        <env name="APP_KEY"      value="base64:dGVzdF9rZXlfMzJfY2hhcnNfZm9yX3Rlc3Rpbmch"/>
        <env name="APP_URL"      value="http://localhost:8080"/>
        <env name="APP_TIMEZONE" value="Asia/Dhaka"/>

        <!-- Database: SQLite in-memory for all tests -->
        <env name="DB_CONNECTION" value="sqlite"/>
        <env name="DB_DATABASE"   value=":memory:"/>

        <!-- Cache / Session / Queue: array drivers (no external services needed) -->
        <env name="CACHE_DRIVER"   value="array"/>
        <env name="SESSION_DRIVER" value="array"/>
        <env name="QUEUE_DRIVER"   value="sync"/>

        <!-- Mail: log driver (emails captured to log, never sent) -->
        <env name="MAIL_DRIVER" value="log"/>
        <env name="MAIL_MAILER" value="log"/>

        <!-- Redis: point at a local test instance; tests that use Redis
             should use the array/null cache driver via env override. -->
        <env name="REDIS_HOST"     value="127.0.0.1"/>
        <env name="REDIS_PORT"     value="6379"/>
        <env name="REDIS_PASSWORD" value=""/>
        <env name="REDIS_DB"       value="15"/>

        <!-- JWT: deterministic 64-char test secret -->
        <env name="JWT_SECRET"      value="test_secret_key_64_chars_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx"/>
        <env name="JWT_ALGO"        value="HS256"/>
        <env name="JWT_TTL"         value="60"/>
        <env name="JWT_REFRESH_TTL" value="20160"/>
        <env name="JWT_BLACKLIST"   value="false"/>

        <!-- Auth / Security -->
        <env name="MAX_LOGIN_ATTEMPTS" value="5"/>
        <env name="LOCKOUT_DURATION"   value="900"/>
        <env name="SESSION_LIFETIME"   value="120"/>
        <env name="MFA_ENABLED"        value="false"/>

        <!-- VAT -->
        <env name="VAT_ENABLED"      value="true"/>
        <env name="DEFAULT_VAT_RATE" value="15"/>

        <!-- Rate limiting (relaxed for tests) -->
        <env name="RATE_LIMIT_REQUESTS" value="1000"/>
        <env name="RATE_LIMIT_WINDOW"   value="60"/>
    </php>

</phpunit>
